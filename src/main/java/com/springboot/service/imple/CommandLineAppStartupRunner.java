package com.springboot.service.imple;

/*
 * import java.util.Arrays;
 * 
 * import org.springframework.beans.factory.annotation.Autowired; import
 * org.springframework.boot.CommandLineRunner; import
 * org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder; import
 * org.springframework.stereotype.Component;
 * 
 * import com.springboot.dao.*; import com.springboot.entities.Role; import
 * com.springboot.entities.User;
 * 
 * 
 * 
 * @Component public class CommandLineAppStartupRunner implements
 * CommandLineRunner {
 * 
 * @Autowired UserRepository userRepository;
 * 
 * @Autowired RoleRepository roleRepository;
 * 
 * @Override public void run(String...args) throws Exception { User user = new
 * User(); Role admin= new Role(); admin.setName("ROLE_ADMIN"); Role prof= new
 * Role(); prof.setName("ROLE_PROFFESSEU"); Role chefF= new Role();
 * chefF.setName("ROLE_CHEFFILIERE"); user.setFirstName("admin");
 * user.setLastName("admin"); user.setCin("none");
 * user.setEmail("admin@gmail.com"); user.setRoles(Arrays.asList(admin));
 * userRepository.save(user); roleRepository.save(admin);
 * roleRepository.save(prof); roleRepository.save(chefF); } }
 */